// Production environment - would point to deployed API
export const environment = {
  production: true,
  apiUrl: 'https://localhost:44327/api'  // For now same as dev, would change in production
};
